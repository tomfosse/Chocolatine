name: Chocolatine project
on: [push, delete]

jobs:
  check_remote_is_valid:
    runs-on: ubuntu-latest
    steps:
      - name: Check the repository's name
        run: |
          if [ "${{ github.ref_name }}" = "no-ga" ]; then
            echo "::error title=Branch's name isn't valid::Please rename your branch"; 
          fi
      - name : Check if it's the same repository
        run: |
          if [ "${{ github.event.repository.ssh_url }}" = "${{vars.MIRROR_SSH_URL}}" ]; then
            echo "::error title=Repository's name is the same as the target::Please rename your remote repository"; 
          fi
  check_compilation_and_coding_style:
    runs-on: ubuntu-latest
    needs: check_remote_is_valid
    container:
      image: epitechcontent/epitest-docker
    steps:
      - uses: actions/checkout@v6.0.2
        with:
          fetch-depth: 0
      - name: check_infractions
        run: banana-check-repo
      - name: compilation
        run: make
        working-directory: ${{ github.workspace }}
        timeout-minutes: 2
  clone_to_branch:
    runs-on: ubuntu-latest
    needs: [check_remote_is_valid, check_compilation_and_coding_style]
    steps:
      - uses: actions/checkout@v6.0.2
        with:
          fetch-depth: 0
      - uses: pixta-dev/repository-mirroring-action@v1
        with:
          target_repo_url:
            ${{vars.MIRROR_SSH_URL}}
          ssh_private_key:
            ${{ secrets.SSH_PRIVATE_KEY }}